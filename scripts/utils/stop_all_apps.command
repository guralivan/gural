#!/bin/bash

# Скрипт для остановки всех приложений
echo "🛑 Остановка всех приложений..."

# Останавливаем все процессы Streamlit
pkill -f "streamlit run"

# Останавливаем процессы по портам
echo "🔌 Остановка процессов на портах 8508-8516..."
for port in {8508..8516}; do
    lsof -ti:$port | xargs kill -9 2>/dev/null
done

# Удаляем PID файлы
rm -f .*.pid

echo "✅ Все приложения остановлены!"
echo "🌐 Порт 8508 освобожден для Календаря производства"
