#!/bin/bash

# Команда для запуска ИИ-аналитика данных
# Автор: ИИ-ассистент
# Дата: $(date)

echo "🤖 Запуск ИИ-аналитика данных..."
echo "================================"

# Переходим в директорию проекта
cd "$(dirname "$0")"

# Проверяем наличие виртуального окружения
if [ ! -d "venv" ]; then
    echo "⚠️ Виртуальное окружение не найдено. Создаю..."
    python3 -m venv venv
fi

# Активируем виртуальное окружение
echo "🔄 Активация виртуального окружения..."
source venv/bin/activate

# Устанавливаем зависимости
echo "📦 Установка зависимостей..."
pip install streamlit pandas plotly numpy openpyxl

# Используем порт 8515
PORT=8515

echo "🚀 Запуск ИИ-аналитика на порту $PORT..."
echo "📊 Откройте браузер: http://localhost:$PORT"
echo "================================"

# Запускаем приложение
streamlit run ai_analyst.py --server.port $PORT --server.address localhost

echo "🤖 ИИ-аналитик остановлен"
