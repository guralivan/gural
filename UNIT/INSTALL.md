# 🔧 Установка и настройка приложения "Юнит-экономика"

## 📋 Предварительные требования

### 1. Python 3.8+
```bash
python --version
```

### 2. pip (менеджер пакетов Python)
```bash
pip --version
```

## 🚀 Установка

### Шаг 1: Создание виртуального окружения
```bash
# Создание виртуального окружения
python -m venv .venv

# Активация виртуального окружения
# Для macOS/Linux:
source .venv/bin/activate

# Для Windows:
# .venv\Scripts\activate
```

### Шаг 2: Установка зависимостей
```bash
# Установка всех необходимых пакетов
pip install -r requirements_unit_economics.txt
```

### Шаг 3: Проверка установки
```bash
# Проверка доступности Streamlit
python -c "import streamlit; print('✅ Streamlit установлен')"

# Проверка других зависимостей
python -c "import pandas, plotly.express; print('✅ Все зависимости установлены')"
```

## 🎯 Запуск приложения

### Вариант 1: Прямой запуск
```bash
streamlit run unit_economics_products_table_FINAL.py
```

### Вариант 2: Через файл запуска (macOS)
```bash
./launch_unit_economics_FINAL.command
```

### Вариант 3: С указанием порта
```bash
streamlit run unit_economics_products_table_FINAL.py --server.port 8505
```

## 📁 Структура данных

Приложение создает и использует следующие папки:
- `saved_products/` - основные товары
- `test_products/` - тестовые товары  
- `cost_products/` - расчеты себестоимости

## 🔧 Устранение неполадок

### Ошибка "ModuleNotFoundError"
```bash
# Переустановка зависимостей
pip install --upgrade -r requirements_unit_economics.txt
```

### Ошибка "Permission denied" при запуске .command файла
```bash
# Установка прав на выполнение
chmod +x launch_unit_economics_FINAL.command
```

### Проблемы с виртуальным окружением
```bash
# Удаление и пересоздание
rm -rf .venv
python -m venv .venv
source .venv/bin/activate
pip install -r requirements_unit_economics.txt
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте версию Python (должна быть 3.8+)
2. Убедитесь, что виртуальное окружение активировано
3. Проверьте установку всех зависимостей
4. Обратитесь к основному README.md

---
**Дата:** 27.08.2024  
**Версия:** Финальная








