# 📋 Инструкция по запуску приложений

## 🚀 Быстрый запуск

### Вариант 1: Запуск всех приложений сразу
1. Дважды кликните на файл `ЗАПУСК_ВСЕХ_ПРИЛОЖЕНИЙ.command`
2. Дождитесь запуска всех приложений
3. Откройте браузер и перейдите по нужному адресу

### Вариант 2: Запуск отдельного приложения
1. Дважды кликните на нужный файл:
   - `ЗАПУСК_КАЛЬКУЛЯТОРА_БАЛАНСА.command` - для калькулятора баланса
2. Откройте браузер и перейдите по адресу

## 🌐 Доступные приложения

| Приложение | Порт | Адрес | Описание |
|------------|------|-------|----------|
| 📊 Основное приложение | 8501 | http://localhost:8501 | Основной дашборд |
| 📈 Сравнение 2024 vs 2025 | 8502 | http://localhost:8502 | Сравнение заказов по годам |
| 📦 Калькулятор баланса | 8503 | http://localhost:8503 | Калькулятор баланса товаров |
| 🏭 Юнит экономика | 8504 | http://localhost:8504 | Расчет юнит экономики |

## ⚙️ Ручной запуск через терминал

```bash
# Перейти в директорию проекта
cd /Users/ivangural/Downloads/wb_dashboard_streamlit

# Активировать виртуальное окружение
source venv/bin/activate

# Запустить нужное приложение
streamlit run app.py --server.port 8501 --server.address localhost
streamlit run app_45_simple.py --server.port 8502 --server.address localhost
streamlit run app_45_simple.py --server.port 8503 --server.address localhost
streamlit run unit_economics_products_table_FINAL.py --server.port 8504 --server.address localhost
```

## 🛑 Остановка приложений

### При использовании .command файлов:
- Нажмите `Ctrl+C` в терминале
- Или закройте терминальное окно

### При ручном запуске:
- Нажмите `Ctrl+C` в каждом терминале
- Или выполните: `pkill -f streamlit`

## 📝 Особенности калькулятора баланса

### ✅ Функции:
- **Автоматическая загрузка данных** из основного приложения
- **Выбор диапазона анализа** с сохранением выбора
- **Расчет оптимального баланса** с учетом возвратов
- **Визуализация данных** в виде графиков
- **Экспорт результатов** в CSV

### 🔧 Настройки:
- **Процент выкупа** (по умолчанию 20%)
- **Начальный баланс** (по умолчанию 500)
- **Выбор периода** для анализа

### 📊 Анализ:
- **Простой расчет** - общее количество заказов
- **С учетом возвратов** - заказы минус выкупленные товары
- **Оптимальный баланс** - минимальный баланс для избежания дефицита
- **Баланс на конец периода** - ожидаемый баланс после всех операций

## 🆘 Решение проблем

### Порт уже используется:
```bash
# Найти процесс
lsof -i :8503

# Остановить процесс
kill -9 <PID>
```

### Ошибки при запуске:
1. Убедитесь, что виртуальное окружение активировано
2. Проверьте, что все зависимости установлены
3. Убедитесь, что файлы приложений существуют

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в терминале
2. Убедитесь, что все файлы на месте
3. Попробуйте перезапустить приложение
