# 🚀 Инструкция по запуску приложений

## 📋 Доступные приложения

### 1. 📊 Анализ 45.xlsx (Порт 8509)
- **Файл:** `app_45_simple.py`
- **Порт:** 8509
- **URL:** http://localhost:8509
- **Описание:** Анализ данных из Excel файла с возможностью загрузки дополнительных отчетов

### 2. 📋 Gotov (Порт 8501)
- **Файл:** `gotov.py`
- **Порт:** 8501
- **URL:** http://localhost:8501
- **Описание:** Приложение для анализа готовых товаров

### 3. 📅 Календарь производства (Порт 8506)
- **Файл:** `production_calendar.py`
- **Порт:** 8506
- **URL:** http://localhost:8506
- **Описание:** Календарь планирования производства

## 🚀 Способы запуска

### Вариант 1: Запуск отдельного приложения
```bash
# Для приложения анализа 45.xlsx
./launch_app_45_8509.command

# Или вручную
streamlit run app_45_simple.py --server.port 8509 --server.address localhost
```

### Вариант 2: Запуск всех приложений одновременно
```bash
./launch_all_apps.command
```

### Вариант 3: Ручной запуск
```bash
# Активируем виртуальное окружение
source venv/bin/activate

# Запускаем приложения на разных портах
streamlit run app_45_simple.py --server.port 8509 --server.address localhost &
streamlit run gotov.py --server.port 8501 --server.address localhost &
streamlit run production_calendar.py --server.port 8506 --server.address localhost &
```

## 🛑 Остановка приложений

### Остановка всех приложений
```bash
pkill -f streamlit
```

### Остановка конкретного порта
```bash
# Найти процесс на порту
lsof -ti:8509

# Остановить процесс
lsof -ti:8509 | xargs kill -9
```

## 📁 Структура файлов

```
wb_dashboard_streamlit/
├── app_45_simple.py              # Основное приложение анализа
├── gotov.py                      # Приложение Gotov
├── production_calendar.py        # Календарь производства
├── launch_app_45_8509.command   # Скрипт запуска анализа на 8509
├── launch_all_apps.command      # Скрипт запуска всех приложений
├── requirements_app_45.txt       # Зависимости для анализа
├── venv/                        # Виртуальное окружение
└── data_cache.csv               # Кеш данных (создается автоматически)
```

## ⚠️ Важные замечания

1. **Порты:** Убедитесь, что порты 8501, 8506, 8509 свободны
2. **Виртуальное окружение:** Все приложения используют одно виртуальное окружение
3. **Зависимости:** Установите зависимости командой `pip install -r requirements_app_45.txt`
4. **Файлы данных:** Приложение анализа 45.xlsx требует файл `45.xlsx` в корневой папке

## 🔧 Устранение неполадок

### Порт уже занят
```bash
# Остановить процесс на порту
lsof -ti:8509 | xargs kill -9
```

### Ошибка импорта модулей
```bash
# Переустановить зависимости
pip install -r requirements_app_45.txt
```

### Приложение не запускается
```bash
# Проверить логи
streamlit run app_45_simple.py --server.port 8509 --server.address localhost

# Проверить права доступа к файлам
chmod +x *.command
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте, что все файлы находятся в правильных папках
2. Убедитесь, что виртуальное окружение активировано
3. Проверьте, что порты не заняты другими процессами
4. Убедитесь, что установлены все зависимости
